/* global describe it expect */
describe('\n - Aplicación: Academico\n', () => {
  describe(` Acción: crear servicio`, () => {
    it('Instanciando un servicio sin parámetros', () => {
      expect(true).to.equal(true)
    })
  })
})
