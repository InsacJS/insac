{
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "start-production": "export NODE_ENV=production && node app.js",
    "setup": "insac migrate && insac seed",
    "setup-production": "export NODE_ENV=production && insac migrate && insac seed",
    "apidoc": "insac apidoc"
  }
}
